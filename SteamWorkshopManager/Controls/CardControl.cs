using Microsoft.UI.Xaml.Controls;

namespace SteamWorkshopManager.Controls;

public sealed class CardControl : ContentControl
{
    public CardControl()
    {
        DefaultStyleKey = typeof(CardControl);
    }
}